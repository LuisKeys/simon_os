model:
  default: ollama
  fallback: local
  api_type: ollama
  host: http://localhost:11434
  model_id: lfm2:24b
  timeout_seconds: 60
  use_cli: false

memory:
  type: sqlite
  path: ./data/memory.db

security:
  require_approval: true
  allow_shell: false

workspace:
  root: .
